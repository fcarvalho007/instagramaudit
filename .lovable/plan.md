

## Plan: Update LOCKED_FILES.md

Update `/LOCKED_FILES.md` to reflect the actual Tailwind v4 structure:
- Replace the `tailwind.config.ts` and `/src/index.css` references with `/src/styles.css` and `/src/routes/__root.tsx`
- Add a note about Tailwind CSS v4 with `@theme inline` at the top

Single file change, no other modifications needed.

